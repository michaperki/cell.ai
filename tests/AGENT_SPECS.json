{
  "tests": [
    { "file": "test_29_ai_agent_city_cleanup.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Normalize the City names in the selection: trim whitespace and convert to Title Case (e.g., 'lincroft' -> 'Lincroft', 'RIDGEWOOD' -> 'Ridgewood', 'CHERRY HILL' -> 'Cherry Hill'). Do not change any other columns. Use set_values only.", "location": "B2:B12", "apply": false },
      { "action": "ai_agent", "prompt": "Normalize the City names in the selection: trim whitespace and convert to Title Case (e.g., 'lincroft' -> 'Lincroft', 'RIDGEWOOD' -> 'Ridgewood', 'CHERRY HILL' -> 'Cherry Hill'). Do not change any other columns. Use set_values only.", "location": "B2:B12", "apply": true }
    ]},
    { "file": "test_30_ai_agent_observe.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Look at the selection and produce observations only (uniques, column profile, sample rows). Do not make any changes.", "location": "A1:C6", "apply": false, "ExpectNoWrites": true, "ExpectTranscript": true, "MaxChangesOutside": 0 }
    ]},
    { "file": "test_31_ai_agent_values_only_gating.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Normalize the City names in the selection: trim whitespace and convert to Title Case. Use set_values only. Do not add titles.", "location": "A2:A6", "apply": false },
      { "action": "ai_agent", "prompt": "Normalize the City names in the selection: trim whitespace and convert to Title Case. Use set_values only. Do not add titles.", "location": "A2:A6", "apply": true }
    ]},
    { "file": "test_32_ai_agent_transform_range.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Normalize the City names in the selection: trim whitespace and convert to Title Case. You may use transform_range with op=normalize_city. Make changes only within the selection.", "location": "A2:A6", "apply": true, "AllowedCommands": ["transform_range"], "MinChangesTotal": 5, "MaxChangesOutside": 0 }
    ]},
    { "file": "test_33_ai_agent_observe_only_strict.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Observe only: return uniques, profile, and a few sample rows. Do not propose or apply any write commands.", "location": "A1:C5", "apply": false, "ExpectNoWrites": true, "ExpectTranscript": true, "MaxChangesOutside": 0 }
    ]},
    { "file": "test_34_ai_agent_selection_fencing.workbook.json", "steps": [
      { "action": "ai_agent", "prompt": "Within the selection only, normalize values by trimming whitespace and converting to Title Case. Use set_values only. Do not touch cells outside the selection.", "location": "B2:B4", "apply": true, "AllowedCommands": ["set_values", "transform_range"], "MaxChangesOutside": 0 }
    ]}
  ]
}

